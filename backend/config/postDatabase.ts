/*
 * nothing in here for now, currently the post database is the default database
 * in mongoose so we can just use mongoose.connect in main server.ts file
 */

// import mongoose, { ConnectOptions } from "mongoose";
// // Connect to Accounts database (profiles database)
// const postConnection = mongoose.createConnection(
// 	"mongodb+srv://freebites7:1234@freebites.w7sk83d.mongodb.net/Posts?retryWrites=true&w=majority",
// 	{
// 		useNewUrlParser: true,
// 		useUnifiedTopology: true,
// 	} as ConnectOptions
// );

// export default postsConnection;
