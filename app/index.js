
import { useState } from 'react';
import { View, Text, SafeAreaView } from 'react-native'; // views are divs and text a p tags
import { COLORS, icons, images, SIZES } from '../constants';

const Home = () => {

    return (
        <SafeAreaView>
            <Text> TEST WOOT </Text>
        </SafeAreaView>
    )

}


export default Home;