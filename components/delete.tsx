import React, { useEffect, useState } from 'react';
import { SafeAreaView, View, Text } from 'react-native';
import axios from 'axios';

const Delete = () => {
        /* Find the desired post via itemId then remove it */
};

export default Delete;